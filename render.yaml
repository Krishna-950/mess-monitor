services:
  - type: web
    name: mess-monitor
    env: python
    buildCommand: "apt-get install -y build-essential cmake libopenblas-dev liblapack-dev && pip install -r requirements.txt"
    startCommand: "python app.py"